<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand h2-font" href="#">KICKBALL MANAGER</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <% if user_signed_in? %>
      <li class="nav-item active">
        <div class="nav-link"><%= link_to "Dashboard", root_path %></div>
      </li>
      
      <li class="nav-item active">
        <div class="nav-link"><%= link_to 'Edit profile', edit_user_registration_path %></div>
      </li>
      
      <li class="nav-item active">
        <div class="nav-link"><%= link_to "Logout", destroy_user_session_path, method: :delete %></div>
      </li>
      
      <% else %>
      <li class="nav-item active">
        <div class="nav-link"<%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
        <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %></div>
      </li>
      <% end %>
    </ul>
  </div>
</nav>